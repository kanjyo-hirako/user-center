# DAY1

- 其实这是前几天的事的结合，个人主前端，所以后端项目是直接copy别人的代码，实际上这个项目也是参照b站上的一个博主做的(yupi)
- 后端有很多的不懂，像什么spring,maven等等的后端技术栈都是一窍不通后端有很多不懂，像什么Spring、Maven等等的后端技术栈都是一窍不通
  所以在强大的codex的帮助下成功跑通了后端的项目(累...)
- 这几天最主要解决的就是前后端常见的跨域问题，主要是前后端的端口不一导致的，导致前端发送的请求后端无法接收
- 然后现在就是卡在用户权限这一块，无论是管理员用户还是普通用户都看不了用户管理页面的用户列表
- 最后是满课的今天，为了更好地区分前后端项目，便将之前创建没几天的仓库删除了(删库跑路这一块)，现在是两个分别存前后端的文件夹
- 我也不知道这对不对...

# DAY2

- 只有一天的周末，把页面之间的逻辑优化了一下，像点击“关于我们”后会停留在一个空白页一样，以及顶部栏对于当前页面定位的问题
- 充分采用了ai技术完善了前后端的README，毕竟以前没有用过README
- 接下来要做的便是完善用户管理页面的功能，以及整个项目功能性的定位
- 已经是凌晨一点半了，明早还有早八，先睡了

# DAY3

- 还是继续优化了页面逻辑，像登录后的跳转，以及进一步优化了用户的体验
- 说的冠冕堂皇，其实今天做的更多的是改错，各种低级的小错误，但在claude大人的面前都迎刃而解了
- 更新了用户管理页面和注册页面，因为搜索功能用的是username，所以就变着法在注册页面加上了定义username的功能，相应的，id变成自增，不用再手动设置了
- 尝试为用户管理页面增加新的功能，新增的头像上传功能似乎不是很好用（照片可上传但不能显示），需要后续的优化
- 修改了顶部栏的逻辑，首次刷新页面时的页面定位，右边“登录”按钮的展示，以及“退出登录”按钮的增加
- 最后就是期待后续的优化了，我感觉自己还不是完全了解这个项目结构的，ai用多了也不是很好啊（笑）
- bird forever fly（摘自b站一个年更up主）

# DAY4

- 今天是满课的一天
- 今天没有在忙这个项目，而是跑去优化github的个人主页了，主要是采用html去渲染markdown文件，以及一些脚本的构建
- 明天要做的是用codex将用户管理中心的头像无法显示的问题解决，其次是思考下一步的拓展和优化
- 质疑自己是常有的事，但是停下来就不对了

# DAY5

- 今天将整个项目都测试了好几遍，总算万无一失了
- 将头像模块进行了改动，可以随意地裁剪上传的头像了
- 目前打算使用vercel部署前端项目，使用rander部署后端java的项目，祝我成功吧
- 想出门的时候总是坏天气，运气和想法总是不在一条线上...

# DAY6

- 今天尝试部署前后端的项目，前端使用了vercel来部署，目前域名为https://kanjyo-hirako-user-center.vercel.app/
- 后端的部署似乎有点困难，项目是打算部署在rander.com的，数据库又要在另一个地方部署（晕晕）
- 今天将登录时遇到的小问题解决了
- 未来的打算是巩固vue3以及前端三件套的基础，有时间可以学学typescript
- 今天没什么话可以送给自己的，早点睡吧（实则是想不到了...）
